# Q1. Write a R program to find the maximum and the minimum value of a givenvecto

vec <- c(1,2,3,4)
print(m1)
min_val <- min(vec)
max_val <- max(vec)
print(paste("minimun value :",min_val))
print(paste("max value:",max_val))

